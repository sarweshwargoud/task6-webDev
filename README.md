# Task 6 — Contact Form with Validation

This project includes a contact form built using **HTML, CSS, and JavaScript** with client-side validation.

## Features
- Name, Email, Message input fields
- Email validation using Regex
- Dynamic error messages
- `event.preventDefault()` used to stop invalid submission
- Success message on valid input

## How to Run
Just open `index.html` in a browser.

## What I Learned
- DOM Manipulation  
- Input validation  
- Regex for email format  
- Preventing form submission in JS  
- User feedback handling

